/* Procedure
1)create two more arrays called top(whose size is equal to no. of stacks) and next
Considering two stacks here(s0 and s1), then top[0] will store so's top element's index and
top[1] will store s1's top element's index
next array: if main array is empty, then it stores the index of the next element which is empty in the next array, and if main array is 
not empty, then next stores the vaalue of the element that came previously in the stack
added to main array,   
*/
